#include "syntaxme.h"

singleton ThreadSafe(int aSingleValue, int bSingleValue, {
    accessor({
        int aSingleValue;
        int bSingleValue;
    });

    (void) self |> "add" |> {
        self->aSingleValue + self->bSingleValue;
    }
});