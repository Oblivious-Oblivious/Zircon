#define object
#define model
#define protocol

#define init(stuff)
#define defer(stuff)
#define fields(stuff);

#define implements
#define self
#define super
#define message
#define supermessage

struct Any;

struct Object;
#define Object(stuff)
struct Object_model;
#define Object_Model(stuff)
struct Object_Protocol;
#define Object_Protocol(stuff)

struct Composition;
#define Composition(stuff)
struct Composition_Model;
#define Composition_Model(stuff)
struct Composition_Protocol;
#define Composition_Protocol(stuff)

struct Point;
#define Point(stuff)
struct Point_model;
#define Point_Model(stuff)
struct Point_Protocol;
#define Point_Protocol(stuff)

struct Test;
#define Test(stuff)