#include <stdio.h>

int main(void) {
    int a = 1;
    int b = 1;
    char *c = "inner";
    char *d = "outer";

    if(a)
        if(b) printf("%s\n", c);
        else printf("%s\n", d);

    a = 0;

    if(a)
        if(b) printf("%s\n", c);
        else printf("%s\n", d);
}
